services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ../data:/app/data
    restart: unless-stopped
